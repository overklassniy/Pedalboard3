#pragma once
#ifndef BINARY_VECTORS_H
    #define BINARY_VECTORS_H

/// Provides access to embedded SVG vector graphics used for UI buttons.
namespace Vectors {
/// The add mapping button SVG data.
extern const char* addmappingbutton_svg;
/// Size in bytes of the addmappingbutton_svg data.
const int addmappingbutton_svgSize = 2588;

/// The add mapping button (hover state) SVG data.
extern const char* addmappingbuttonover_svg;
/// Size in bytes of the addmappingbuttonover_svg data.
const int addmappingbuttonover_svgSize = 2594;

/// The bypass button (off state) SVG data.
extern const char* bypassbuttonoff_svg;
/// Size in bytes of the bypassbuttonoff_svg data.
const int bypassbuttonoff_svgSize = 3349;

/// The bypass button (on state) SVG data.
extern const char* bypassbuttonon_svg;
/// Size in bytes of the bypassbuttonon_svg data.
const int bypassbuttonon_svgSize = 3050;

/// The close filter button SVG data.
extern const char* closefilterbutton_svg;
/// Size in bytes of the closefilterbutton_svg data.
const int closefilterbutton_svgSize = 2821;

/// The close filter button (pressed state) SVG data.
extern const char* closefilterbuttondown_svg;
/// Size in bytes of the closefilterbuttondown_svg data.
const int closefilterbuttondown_svgSize = 2864;

/// The close filter button (hover state) SVG data.
extern const char* closefilterbuttonover_svg;
/// Size in bytes of the closefilterbuttonover_svg data.
const int closefilterbuttonover_svgSize = 2820;

/// The delete button SVG data.
extern const char* deletebutton_svg;
/// Size in bytes of the deletebutton_svg data.
const int deletebutton_svgSize = 2809;

/// The new button SVG data.
extern const char* newbutton_svg;
/// Size in bytes of the newbutton_svg data.
const int newbutton_svgSize = 2266;

/// The open button SVG data.
extern const char* openbutton_svg;
/// Size in bytes of the openbutton_svg data.
const int openbutton_svgSize = 3357;

/// The output toggle button (state 1) SVG data.
extern const char* outputtoggle1_svg;
/// Size in bytes of the outputtoggle1_svg data.
const int outputtoggle1_svgSize = 2373;

/// The output toggle button (state 2) SVG data.
extern const char* outputtoggle2_svg;
/// Size in bytes of the outputtoggle2_svg data.
const int outputtoggle2_svgSize = 2375;

/// The pause button SVG data.
extern const char* pausebutton_svg;
/// Size in bytes of the pausebutton_svg data.
const int pausebutton_svgSize = 2159;

/// The play button SVG data.
extern const char* playbutton_svg;
/// Size in bytes of the playbutton_svg data.
const int playbutton_svgSize = 2104;

/// The record button SVG data.
extern const char* recordbutton_svg;
/// Size in bytes of the recordbutton_svg data.
const int recordbutton_svgSize = 2130;

/// The return-to-zero button SVG data.
extern const char* rtzbutton_svg;
/// Size in bytes of the rtzbutton_svg data.
const int rtzbutton_svgSize = 2363;

/// The save button SVG data.
extern const char* savebutton_svg;
/// Size in bytes of the savebutton_svg data.
const int savebutton_svgSize = 2532;

/// The stop button SVG data.
extern const char* stopbutton_svg;
/// Size in bytes of the stopbutton_svg data.
const int stopbutton_svgSize = 1991;

}; // namespace Vectors

#endif
